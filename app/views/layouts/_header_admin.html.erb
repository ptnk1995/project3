_<nav class="navbar navbar-default margin-navbar">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to admin_root_path do %>
        <%= image_tag "/assets/logo.png", class: "logo-admin" %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse navbar-custom"
      id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <ul class="nav navbar-nav text-transform-none ">
            <li class="dropdown">
              <%= link_to "", class: "dropdown-toggle", data: {toggle: "dropdown"} do %>
                <%= current_user.name %>
                <span class="glyphicon glyphicon-user pull-right"></span>
              <% end %>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to user_path(I18n.locale, current_user) do %>
                    <%= t "profile" %>
                    <span class="glyphicon glyphicon-user pull-right"></span>
                  <% end %>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to edit_user_path(I18n.locale, current_user) do %>
                    <%= t "menu_edit_profile" %>
                    <span class="glyphicon glyphicon-cog pull-right"></span>
                  <% end %>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to destroy_user_session_path(locale: I18n.locale), method: :delete do %>
                    <%= t "log_out" %>
                    <span class="glyphicon glyphicon-log-out pull-right"></span>
                  <% end %>
                </li>
              </ul>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
